// It's not towel ;)

#ifndef RAG_H
# define RAG_H

#include <assert.h>

#define ASSERT_STRINGS_MATCH(X, Y) assert(strcmp(X, Y) == 0)
#define ASSERT_EQUAL(X, Y) assert(X == Y)
#define ASSERT_NOT_EQUAL(X, Y) assert(X != Y)

#endif
